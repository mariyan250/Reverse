// React
import React from 'react';

// Loading
import ReactLoading from 'react-loading';

function Loading() {
  return <ReactLoading type="spin" color="black" width={50} height={50} />;
}

export default Loading;
